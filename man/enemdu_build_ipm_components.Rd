% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ipm_components.R
\name{enemdu_build_ipm_components}
\alias{enemdu_build_ipm_components}
\title{Build registered IPM component indicators}
\usage{
enemdu_build_ipm_components(
  data,
  profile = "enemdu_2025_anual",
  household_data = NULL,
  household_id = "id_hogar",
  person_id = "p01",
  hsize_var = "hsize",
  overwrite = FALSE,
  strict = TRUE,
  extreme_poverty_var = "expobre",
  build_from_precomputed = TRUE,
  water_public_network_codes = 1
)
}
\arguments{
\item{data}{Person-level ENEMDU data.}

\item{profile}{IPM derivation profile. Defaults to \code{"enemdu_2025_anual"}.}

\item{household_data}{Optional household- or housing-level ENEMDU data. When
supplied, it is joined with \code{enemdu_join_ipm_sources()}.}

\item{household_id}{Household identifier.}

\item{person_id}{Person identifier.}

\item{hsize_var}{Household-size variable. If absent, household size is
derived by counting persons within \code{household_id} for implemented household
components.}

\item{overwrite}{Logical. If \code{TRUE}, replace components that can be built
from implemented rules.}

\item{strict}{Logical. If \code{TRUE}, abort when any registered component remains
pending or when implemented source values are invalid or missing.}

\item{extreme_poverty_var}{Precomputed binary extreme-income-poverty flag.
Defaults to \code{"expobre"}.}

\item{build_from_precomputed}{Logical. If \code{TRUE}, existing registered
component columns are accepted as precomputed inputs after binary validation.}

\item{water_public_network_codes}{Codes that identify public-network water in
the source water variable. The default \code{1} is profile-specific and still
requires real-data reproducibility validation.}
}
\value{
A data frame with the 12 registered IPM component columns when
\code{strict = FALSE} or when all components are available, plus an
\code{ipm_component_diagnostics} attribute.
}
\description{
Builds the IPM component columns that are auditable in the current package
contract and marks the remaining registered components as pending. The
current operational rules cover public-network water, overcrowding, and
extreme income poverty when a precomputed binary poverty flag is supplied.
}
\details{
This function does not invent rules for education, labor, pensions, housing
deficit, sanitation, or garbage collection. When \code{strict = TRUE}, the
function aborts if the full set of 12 registered IPM components cannot be
completed from implemented rules or accepted precomputed component columns.
When \code{strict = FALSE}, pending components are returned as \code{NA_integer_} and
documented in the diagnostics attribute.

This function does not compute \code{ipm_score}, \code{tpm}, \code{tpem}, \code{A}, or aggregate
\code{ipm}, and it does not claim official validation.
}
