% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ipm_flags.R
\name{enemdu_build_ipm_flags}
\alias{enemdu_build_ipm_flags}
\title{Build IPM score and flags from registered components}
\usage{
enemdu_build_ipm_flags(
  data,
  component_cols = NULL,
  score_var = "ipm_score",
  tpm_var = "tpm",
  tpem_var = "tpem",
  overwrite = FALSE,
  strict = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{component_cols}{Optional character vector with the 12 binary IPM
component columns. If \code{NULL}, the expected component names are read from
\code{ipm_component_registry.csv}.}

\item{score_var}{Output weighted deprivation score variable.}

\item{tpm_var}{Output multidimensional poverty flag variable.}

\item{tpem_var}{Output extreme multidimensional poverty flag variable.}

\item{overwrite}{Logical. If \code{TRUE}, overwrite existing output variables.}

\item{strict}{Logical. If \code{TRUE}, abort on missing component columns, invalid
binary values, or missing component values. If \code{FALSE}, component missing
values are allowed to propagate to \code{ipm_score}, \code{tpm}, and \code{tpem}.}
}
\value{
A data frame with IPM score and flags plus an
\code{ipm_flags_diagnostics} attribute.
}
\description{
Computes row-level IPM weighted deprivation scores and poverty flags from the
12 already-built binary IPM component indicators declared in
\code{ipm_component_registry.csv}. This function does not derive the 12 indicators
from raw ENEMDU questionnaire variables.
}
\details{
This function computes \code{ipm_score}, \code{tpm}, and \code{tpem} only. It does not
compute aggregate \code{A} or aggregate \code{IPM}; those require survey-design-aware
aggregation among multidimensionally poor people and belong to a future KPI
layer. This function does not claim official validation.
}
