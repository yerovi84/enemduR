% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ipm_kpis.R
\name{enemdu_kpi_ipm}
\alias{enemdu_kpi_ipm}
\title{Estimate IPM KPIs from row-level IPM flags}
\usage{
enemdu_kpi_ipm(
  data,
  survey_type = c("anual", "trimestral", "mensual"),
  by = NULL,
  ids = "upm",
  strata = "estrato",
  weight = "fexp",
  score_var = "ipm_score",
  tpm_var = "tpm",
  tpem_var = "tpem",
  build_components = FALSE,
  build_flags = TRUE,
  component_cols = NULL,
  strict = TRUE,
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{survey_type}{One of \code{"anual"}, \code{"trimestral"}, or \code{"mensual"}.}

\item{by}{Optional grouping variable or variables.}

\item{ids}{Primary sampling unit variable.}

\item{strata}{Survey strata variable.}

\item{weight}{Survey expansion factor variable.}

\item{score_var}{Row-level IPM score variable.}

\item{tpm_var}{Row-level multidimensional poverty flag variable.}

\item{tpem_var}{Row-level extreme multidimensional poverty flag variable.}

\item{build_components}{Logical. If \code{TRUE}, call
\code{enemdu_build_ipm_components()} before flags are checked.}

\item{build_flags}{Logical. If \code{TRUE}, build row-level flags from registered
component columns when those components are available.}

\item{component_cols}{Optional registered IPM component columns passed to
\code{enemdu_build_ipm_flags()}.}

\item{strict}{Logical. If \code{TRUE}, abort on missing or invalid score or flag
variables.}

\item{...}{Additional named arguments. Arguments matching
\code{enemdu_build_ipm_components()} are used only when \code{build_components = TRUE};
the remaining arguments are passed to survey estimators.}
}
\value{
A tibble with survey KPI estimates for \code{tpm}, \code{tpem}, \code{A}, and
\code{ipm}, plus non-official validation metadata.
}
\description{
Estimates Ecuador IPM analytical outputs from already-built row-level
variables. The function consumes \code{ipm_score}, \code{tpm}, and \code{tpem}, or builds
them from the 12 registered component columns with
\code{enemdu_build_ipm_flags()}. If requested, it can first call
\code{enemdu_build_ipm_components()}, respecting that builder's pending-component
behavior.
}
\details{
The aggregate outputs are \code{tpm}, \code{tpem}, \code{A}, and \code{ipm}. Here \code{A} is the
average deprivation intensity among multidimensionally poor persons, and
aggregate \code{ipm} is calculated as \code{tpm * A}. These are aggregate KPI outputs,
not row-level variables.

This function does not derive new raw IPM component rules and does not claim
official institutional validation.
}
