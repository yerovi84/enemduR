% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poverty_flags.R
\name{enemdu_build_poverty_flags}
\alias{enemdu_build_poverty_flags}
\title{Build poverty and extreme poverty flags}
\usage{
enemdu_build_poverty_flags(
  data,
  period = NULL,
  income_var = "ingtot_pc",
  poverty_line = NULL,
  extreme_poverty_line = NULL,
  poverty_lines = enemdu_poverty_line_registry(),
  mode = c("strict", "manual", "diagnostic_only"),
  line_source = NULL,
  poverty_var = "pobre",
  extreme_poverty_var = "expobre",
  add_line_vars = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{data}{A data frame.}

\item{period}{Period identifier used to resolve poverty lines in strict mode.}

\item{income_var}{Name of the income per capita variable. Defaults to
\code{"ingtot_pc"}.}

\item{poverty_line}{Manual poverty line. Required in manual mode.}

\item{extreme_poverty_line}{Manual extreme poverty line. Required in manual
mode.}

\item{poverty_lines}{Poverty-line registry. Defaults to package registry.}

\item{mode}{One of \code{"strict"}, \code{"manual"} or \code{"diagnostic_only"}.}

\item{line_source}{Source note required in manual mode.}

\item{poverty_var}{Output poverty flag variable. Defaults to \code{"pobre"}.}

\item{extreme_poverty_var}{Output extreme poverty flag variable. Defaults to
\code{"expobre"}.}

\item{add_line_vars}{Logical. If \code{TRUE}, adds \code{linea_pobreza} and
\code{linea_pobreza_extrema} to the data.}

\item{overwrite}{Logical. If \code{TRUE}, overwrites existing output variables.}
}
\value{
A data frame with poverty flags, or the unchanged data with a
diagnostic attribute when \code{mode = "diagnostic_only"}.
}
\description{
Classifies poverty and extreme poverty using per-capita household income and
explicit poverty-line parameters. In strict mode, poverty lines are resolved
from the poverty-line registry. In manual mode, both poverty lines must be
provided explicitly together with a source note.
}
\details{
This function deliberately refuses to derive poverty indicators from implicit
or non-auditable poverty lines.
}
