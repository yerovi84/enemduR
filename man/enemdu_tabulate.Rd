% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabulate.R
\name{enemdu_tabulate}
\alias{enemdu_tabulate}
\title{Tabulate ENEMDU data}
\usage{
enemdu_tabulate(
  data,
  group_vars = NULL,
  value = NULL,
  weight = "fexp",
  statistic = c("count", "sum", "mean", "proportion"),
  measure = NULL,
  na_rm = TRUE,
  drop_na_groups = TRUE,
  sample_n_min = 60
)
}
\arguments{
\item{data}{A data frame.}

\item{group_vars}{Optional character vector of grouping variables.}

\item{value}{Optional value variable. Required for \code{"sum"}, \code{"mean"} and
\code{"proportion"}.}

\item{weight}{Optional weight variable. Defaults to \code{"fexp"} if present.
If \code{NULL}, unweighted calculations are performed.}

\item{statistic}{One of \code{"count"}, \code{"sum"}, \code{"mean"} or \code{"proportion"}.}

\item{measure}{Optional name for the output measure.}

\item{na_rm}{Logical. If \code{TRUE}, missing values in \code{value} are excluded from
sum, mean, and proportion calculations.}

\item{drop_na_groups}{Logical. If \code{TRUE}, records with missing values in any
grouping variable are excluded.}

\item{sample_n_min}{Minimum unweighted sample size used for a preliminary
sample-size flag. Defaults to \code{60}.}
}
\value{
A tibble with one row per tabulated group.
}
\description{
Produces a stable long-format tabulation using optional grouping variables,
a value variable, and an optional expansion factor.
}
\details{
This function performs basic weighted estimation. It does not yet calculate
standard errors, coefficients of variation, effective sample sizes, degrees
of freedom, or confidence intervals. Those elements belong to the
representativity and precision module.
}
