% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ipm_sources.R
\name{enemdu_join_ipm_sources}
\alias{enemdu_join_ipm_sources}
\title{Join IPM person and household source files}
\usage{
enemdu_join_ipm_sources(
  data,
  household_data,
  household_id = "id_hogar",
  housing_vars = NULL,
  overwrite = FALSE,
  strict = TRUE
)
}
\arguments{
\item{data}{Person-level ENEMDU data.}

\item{household_data}{Household- or housing-level ENEMDU data.}

\item{household_id}{Household identifier used in both sources.}

\item{housing_vars}{Household or housing variables to append from
\code{household_data}. If \code{NULL}, the default IPM source variable set is used.}

\item{overwrite}{Logical. If \code{TRUE}, replace existing \code{housing_vars} in
\code{data}.}

\item{strict}{Logical. If \code{TRUE}, abort when a non-missing household in
\code{data} has no match in \code{household_data}. If \code{FALSE}, unmatched person rows
are preserved and receive \code{NA} housing values.}
}
\value{
A data frame with appended household or housing variables and an
\code{ipm_source_join_diagnostics} attribute.
}
\description{
Appends household or housing variables from a household-level source to a
person-level ENEMDU source using a controlled household-key match. This
function only joins source variables. It does not derive IPM components, does
not compute \code{ipm_score}, \code{tpm}, \code{tpem}, \code{A}, or \code{ipm}, and does not validate
official IPM reproducibility.
}
\details{
The output is intended to prepare person-level data for a future
\code{enemdu_build_ipm_components()} implementation.
}
\examples{
person <- tibble::tibble(
  id_hogar = c("h1", "h1", "h2"),
  p01 = c(1, 2, 1),
  p03 = c(40, 8, 45)
)
household <- tibble::tibble(
  id_hogar = c("h1", "h2"),
  vi03a = c(1, 2),
  vi03b = c(1, 1),
  vi04a = c(7, 1),
  vi04b = c(1, 1),
  vi05a = c(1, 6),
  vi05b = c(1, 1),
  vi07 = c(0, 1),
  vi09 = c(5, 1),
  vi10 = c(1, 2),
  vi13 = c(2, 1)
)

joined <- enemdu_join_ipm_sources(person, household)
joined$vi03a
}
