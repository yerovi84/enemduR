% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_variables.R
\name{enemdu_build_variables}
\alias{enemdu_build_variables}
\title{Build derived ENEMDU variables}
\usage{
enemdu_build_variables(
  data,
  household_id = "idhogar",
  normalize_missing = TRUE,
  missing_applies_to = "income_derivation",
  keep_raw_missing = TRUE,
  create_missing_flags = TRUE,
  missing_vars_absent = c("warn_as_na", "error"),
  existing = c("backup", "overwrite", "error"),
  backup_suffix = "_source"
)
}
\arguments{
\item{data}{A data frame.}

\item{household_id}{Household identifier. Defaults to \code{"idhogar"} and falls
back to \code{"id_hogar"} when needed.}

\item{normalize_missing}{Logical. If \code{TRUE}, applies
\code{enemdu_normalize_missing_values()} to registered income components before
deriving income variables.}

\item{missing_applies_to}{Missing-code registry scope. Defaults to
\code{"income_derivation"}.}

\item{keep_raw_missing}{Logical passed to \code{enemdu_normalize_missing_values()}.}

\item{create_missing_flags}{Logical passed to
\code{enemdu_normalize_missing_values()}.}

\item{missing_vars_absent}{One of \code{"warn_as_na"} or \code{"error"}. If
\code{"warn_as_na"}, absent optional income variables are treated as all-missing
during income construction.}

\item{existing}{One of \code{"backup"}, \code{"overwrite"} or \code{"error"} for existing
output variables.}

\item{backup_suffix}{Suffix used when \code{existing = "backup"}.}
}
\value{
A data frame with derived variables added and derivation metadata in
attributes.
}
\description{
Builds the phase-3 core derived variables for ENEMDU analytical workflows:
}
\details{
\itemize{
\item household size (\code{hsize}),
\item main labor income (\code{ingr}),
\item secondary labor income (\code{ingrls}),
\item total labor income (\code{ingrl}),
\item individual total income before household aggregation (\code{ingrltot}),
\item household total income (\code{ingtot}),
\item household per-capita income (\code{ingtot_pc}).
}

The function deliberately does not derive poverty or extreme poverty flags.
Poverty classification is handled by \code{enemdu_build_poverty_flags()} because it
requires explicit and auditable poverty-line parameters.
}
