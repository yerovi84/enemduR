% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ipm_reproducibility.R
\name{enemdu_run_ipm_reproducibility}
\alias{enemdu_run_ipm_reproducibility}
\title{Run IPM reproducibility workflow}
\usage{
enemdu_run_ipm_reproducibility(
  data,
  period = "2025-12",
  survey_type = "anual",
  by = "area",
  ids = "upm",
  strata = "estrato",
  weight = "fexp",
  build_components = FALSE,
  build_flags = TRUE,
  strict = TRUE,
  tolerance_pp = 0.5,
  missing_component_policy = c("error", "complete_case"),
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{period}{Benchmark period.}

\item{survey_type}{ENEMDU survey type.}

\item{by}{Optional domain variable. Defaults to \code{"area"}.}

\item{ids}{Primary sampling unit variable.}

\item{strata}{Survey strata variable.}

\item{weight}{Survey expansion factor variable.}

\item{build_components}{Logical. If \code{TRUE}, call
\code{enemdu_build_ipm_components()} before flag validation.}

\item{build_flags}{Logical. If \code{TRUE}, build row-level flags from registered
components when available.}

\item{strict}{Logical. If \code{TRUE}, abort on invalid reproducibility inputs.}

\item{tolerance_pp}{Benchmark-comparison tolerance in percentage points.}

\item{missing_component_policy}{Missing IPM evidence policy. \code{"error"}
refuses incomplete registered components or score/flag inputs. \code{"complete_case"}
excludes rows with incomplete registered components, or incomplete score/flag
inputs when components are unavailable, before estimation.}

\item{...}{Additional named arguments passed to \code{enemdu_kpi_ipm()} and,
when relevant, \code{enemdu_build_ipm_components()}.}
}
\value{
A structured list with preflight, validation, estimates, benchmarks,
comparison, complete-case diagnostics, and non-official validation metadata.
}
\description{
Runs a local IPM reproducibility workflow: validates inputs, estimates IPM
KPIs, loads published IPM benchmarks, and compares local estimates to those
benchmarks. It consumes already-built score and flag variables or can build
flags from registered component columns. It does not implement new raw IPM
component rules and does not read microdata files directly.
}
\details{
Benchmarks are published comparison values, not institutional validation.
}
