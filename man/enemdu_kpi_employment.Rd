% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labor_indicators.R
\name{enemdu_kpi_employment}
\alias{enemdu_kpi_employment}
\title{Estimate core ENEMDU employment KPIs using survey design}
\usage{
enemdu_kpi_employment(
  data,
  group_vars = NULL,
  condact = "condact",
  age = "p03",
  sector = "secemp",
  ids = "upm",
  strata = "estrato",
  weight = "fexp",
  survey_type = NULL,
  domain_scope = c("observed", "design"),
  include_totals = TRUE,
  include_rates = TRUE,
  strict = TRUE,
  conf_level = 0.95,
  lonely_psu = "adjust",
  sample_n_min = 60
)
}
\arguments{
\item{data}{A data frame.}

\item{group_vars}{Optional grouping variables.}

\item{condact}{Condition-of-activity variable. Defaults to \code{"condact"}.}

\item{age}{Age variable. Defaults to \code{"p03"}.}

\item{sector}{Employment-sector variable. Defaults to \code{"secemp"}.}

\item{ids}{Primary sampling unit variable. Defaults to \code{"upm"}.}

\item{strata}{Strata variable. Defaults to \code{"estrato"}.}

\item{weight}{Expansion factor variable. Defaults to \code{"fexp"}.}

\item{survey_type}{Optional ENEMDU survey type.}

\item{domain_scope}{Domain filtering policy. \code{"observed"} returns all observed
domains generated by \code{group_vars}. \code{"design"} estimates with the complete data
and then filters the output to recognized ENEMDU design domains for the
declared \code{survey_type}.}

\item{include_totals}{Logical. If \code{TRUE}, estimates labor-population totals.}

\item{include_rates}{Logical. If \code{TRUE}, estimates core labor rates.}

\item{strict}{Logical. If \code{TRUE}, invalid observed labor codes produce an error.}

\item{conf_level}{Confidence level.}

\item{lonely_psu}{Option passed to \code{survey.lonely.psu}.}

\item{sample_n_min}{Minimum unweighted sample size for preliminary quality flag.}
}
\value{
A tibble with survey-design employment KPI estimates.
}
\description{
Estimates labor totals and core rates from the official consolidated ENEMDU
condition-of-activity variable. The function first builds labor flags with
\code{enemdu_build_labor_flags()} and then estimates totals and rates using the
package survey-design estimators.
}
\details{
The default \code{domain_scope = "observed"} preserves observed grouping values in
the output. When \code{domain_scope = "design"}, estimation still uses the full
input microdata and only the output rows are filtered to recognized design
domains for registered grouping variables. For official city-domain
validation against published tabulations, use \code{dominio} when that derived
field is available; \code{ciudad} is not assumed to be equivalent to the published
official city-domain tabulations.
}
