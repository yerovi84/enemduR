% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poverty_kpis.R
\name{enemdu_kpi_income_poverty}
\alias{enemdu_kpi_income_poverty}
\title{Estimate income poverty KPIs from explicit poverty lines}
\usage{
enemdu_kpi_income_poverty(
  data,
  group_vars = NULL,
  income_var = "ingtot_pc",
  period = NULL,
  mode = c("strict", "manual"),
  poverty_line = NULL,
  extreme_poverty_line = NULL,
  poverty_lines = enemdu_poverty_line_registry(),
  line_source = NULL,
  poverty_var = "pobre",
  extreme_poverty_var = "expobre",
  add_line_vars = TRUE,
  overwrite = FALSE,
  registry = enemdu_indicator_registry(),
  official_validation_status = "not_officially_validated",
  official_validation_note =
    "Estimates have not been compared against official INEC tabulations.",
  ...
)
}
\arguments{
\item{data}{A data frame.}

\item{group_vars}{Optional grouping variables.}

\item{income_var}{Household per-capita income variable. Defaults to
\code{"ingtot_pc"}.}

\item{period}{Period identifier used for poverty-line metadata and strict
registry lookup.}

\item{mode}{One of \code{"strict"} or \code{"manual"}.}

\item{poverty_line}{Manual poverty line. Required in manual mode.}

\item{extreme_poverty_line}{Manual extreme poverty line. Required in manual
mode.}

\item{poverty_lines}{Poverty-line registry. Defaults to the package template
registry.}

\item{line_source}{Source note required in manual mode.}

\item{poverty_var}{Output poverty flag variable. Defaults to \code{"pobre"}.}

\item{extreme_poverty_var}{Output extreme poverty flag variable. Defaults to
\code{"expobre"}.}

\item{add_line_vars}{Logical. If \code{TRUE}, adds poverty-line variables to the
working data before estimation.}

\item{overwrite}{Logical. If \code{TRUE}, overwrites existing poverty output
variables.}

\item{registry}{Indicator registry used by \code{enemdu_indicator_estimate()}.}

\item{official_validation_status}{Status label for official validation.
Defaults to \code{"not_officially_validated"}.}

\item{official_validation_note}{Note explaining the official validation
status.}

\item{...}{Additional arguments passed to \code{enemdu_indicator_estimate()}.}
}
\value{
A tibble with income-poverty KPI estimates and poverty-line
provenance metadata.
}
\description{
Builds income-poverty and extreme-income-poverty flags from explicit,
auditable poverty lines and estimates their incidence with the package
survey-design-aware indicator estimator.
}
\details{
This function does not derive official poverty lines, does not ingest CPI
series, and does not claim official validation. Official validation requires
comparing estimates against official INEC tabulations and documenting the
evidence.
}
\examples{
\dontrun{
data <- tibble::tibble(
  idhogar = c("h1", "h1", "h2", "h2"),
  hsize = c(2L, 2L, 2L, 2L),
  upm = c(1, 2, 3, 4),
  estrato = c(1, 1, 2, 2),
  fexp = c(1, 1, 1, 1),
  ingtot_pc = c(40, 75, 125, 150)
)

enemdu_kpi_income_poverty(
  data = data,
  period = "2025-12",
  mode = "manual",
  poverty_line = 100,
  extreme_poverty_line = 50,
  line_source = "Synthetic manual lines for examples; not official validation.",
  survey_type = "anual",
  sample_n_min = 1
)
}
}
