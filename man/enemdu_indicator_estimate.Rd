% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicator_estimate.R
\name{enemdu_indicator_estimate}
\alias{enemdu_indicator_estimate}
\title{Estimate an ENEMDU indicator from the indicator registry}
\usage{
enemdu_indicator_estimate(
  data,
  indicator_id,
  group_vars = NULL,
  registry = enemdu_indicator_registry(),
  value = NULL,
  ids = "upm",
  strata = "estrato",
  weight = NULL,
  survey_type = NULL,
  household_id = "idhogar",
  hsize = "hsize",
  scale_adjustment = c("metadata", "never", "always"),
  conf_level = 0.95,
  lonely_psu = "adjust",
  sample_n_min = 60
)
}
\arguments{
\item{data}{A data frame.}

\item{indicator_id}{Indicator identifier declared in \code{indicator_registry.csv}.}

\item{group_vars}{Optional grouping variables.}

\item{registry}{Indicator registry. Defaults to package registry.}

\item{value}{Optional value variable override.}

\item{ids}{Primary sampling unit variable. Defaults to \code{"upm"}.}

\item{strata}{Strata variable. Defaults to \code{"estrato"}.}

\item{weight}{Optional weight variable override. If \code{NULL}, uses registry
value.}

\item{survey_type}{Optional ENEMDU survey type. If omitted, uses the
\code{survey_type} attribute when available.}

\item{household_id}{Household identifier used when household-scale adjustment
is needed.}

\item{hsize}{Household-size variable.}

\item{scale_adjustment}{One of \code{"metadata"}, \code{"never"} or \code{"always"}.
\code{"metadata"} applies the adjustment only when the registry requests it.}

\item{conf_level}{Confidence level.}

\item{lonely_psu}{Option passed to \code{survey.lonely.psu}.}

\item{sample_n_min}{Preliminary minimum sample size flag.}
}
\value{
A tibble with the design-based estimate and registry metadata.
}
\description{
Estimates an indicator declared in \code{indicator_registry.csv} by selecting the
appropriate survey estimator according to the indicator contract.
}
\details{
Supported estimator types in this phase are:
\itemize{
\item \code{mean}
\item \code{total}
\item \code{proportion_0_1}
}

Indicators with \code{scale_adjustment_required = TRUE} receive an internal
household-scale correction for the precision decision when the adjustment is
enabled. This does not change the point estimate. It adjusts the effective
analytical size used for the representativity decision by summing \code{1 / hsize}
over valid repeated household-level observations.
}
